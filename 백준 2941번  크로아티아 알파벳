#크로아티아 알파벳
#문제를 풀기 위해서 입력받는 문자에서 2글자~3글자로 이루어진 8개의 알파벳이 있는 경우
#한 글자로 변환을 하고서 이후에 변환된 문자열의 총 글자 수를 세는 방법으로 코드를 작성했다
a=["c=","c-","dz=","d-","lj","nj","s=","z="]
alpha=input()

for t in a:
    alpha=alpha.replace(t,'*')#문자를 변환하는 함수는 replace함수를 사용하
print(len(alpha))    
