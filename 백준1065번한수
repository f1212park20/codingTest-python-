#한수
#우선, 1부터 99까지의 수는 모두 등차수열로 구분된다.
#만약 i가 100보다 작다면 han(한수)에 +1을 해준다.
#파이썬은 형변환이 쉽게 가능하다.
#map함수와 list를 사용하여 수를 하나씩 나눠준다.
#첫번째 숫자와 두번째 숫자의 차와
#두번째 숫자와 마지막 숫자의 차이가 같으면
#han(한수)에 1을 더해준다.
#han(한수)를 print 해준다.

n = int(input())
han = 0
for i in range(1, n + 1):
    if i < 100:
        han += 1
    else:
        ns = list(map(int, str(i)))
        if ns[0] - ns[1] == ns[1] - ns[2]:
            han += 1
print(han)
