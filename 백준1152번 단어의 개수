#단어의 개수
#첫 줄에 영어 대소문자와 띄어쓰기로 이루어진 문자열이 주어진다.
#이 문자열의 길이는 1,000,000을 넘지 않는다. 단어는 띄어쓰기 한 개로 구분되며,
#공백이 연속해서 나오는 경우는 없다. 또한 문자열의 앞과 뒤에는 공백이 있을 수도 있다.
n=list(input().split(' '))
while '' in n:
    n.remove('')
if len(n)<1000000:
    print(len(n))
    
#split() 함수로 분해해서 만든 리스트에 ''이라는 형태로 불필요한 요소가 남게 된다.
#그래서 ''가 존재하지 않을 때까지 while 문 내에서 ''을 remove() 함수로 제거하는 작업을 해주어야 한다.    
    
    
    
